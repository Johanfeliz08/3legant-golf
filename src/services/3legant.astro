---

import MockData from "@/utils/MockData.json";

/* How to use */

/* 1. Import the function 
// import { getBy } from "@/services/3legant.astro"

/* Call the function and pass the parameter you want to filter the data by */ 
// Ex: const res = getBy("items");

// Options: 1. "categories", 2. "items"


export const getBy = (param) => {
  
  return MockData[param];

}

/* How to use */

/* 1. Import the function 
// import { getItemsByProperty } from "@/services/3legant.astro"

/* Call the function and pass the parameter you want to filter the data by */ 
// Ex: const res = getItemsByProperty("description");

// Returns all the values of the given property

export const getItemsByProperty = (param) => {

  const data = getBy("items");
  
  const items = data.map((item) => {

      return item[param]

  }); 

  return items

}

/* How to use */

/* 1. Import the function 
// import { getCategoriesBy } from "@/services/3legant.astro"

/* Call the function and pass the parameter you want to filter the data by */ 
// Ex: const res = getCategoriesByProperty("id");

// Returns all the values of the given property

export const getCategoriesByProperty = (param) => {

const data = getBy("categories");

const categories = data.map((category) => {

    return category[param]

}); 

return categories

}

/* How to use */

/* 1. Import the function 
// import { getCategoryBy } from "@/services/3legant.astro"

/* Call the function and pass the parameter you want to filter the data by */ 
// Ex: const res = getCategoryBy("id",1);

// Returns only the object which match the property and value


export const getCategoryBy = (property,value) => {

  const data = getBy("categories");

  return data.find(category => category[property] === value)


}

/* How to use */

/* 1. Import the function 
// import { getItemsBy } from "@/services/3legant.astro"

/* Call the function and pass the parameter you want to filter the data by */ 
// Ex: const res = getItemsBy("id",1);

// Returns only the object which match the property and value


export const getItemBy = (property,value) => {

const data = getBy("items");

return data.find(item => item[property] === value)


}




---
